/** Shared session lifetime: 30 days */
export const SESSION_MAX_AGE_SECONDS = 30 * 24 * 60 * 60;

/** Extend auth session when the user is active (within 24h). */
export const SESSION_UPDATE_AGE_SECONDS = 24 * 60 * 60;
